# Copy to .env in this directory (required for Prisma CLI + IDE validation)
# Use your real Neon URL in apps/api/.env for the running API.

DATABASE_URL="postgresql://postgres:postgres@localhost:5432/nombaflow?schema=public"
